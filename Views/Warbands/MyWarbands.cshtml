@model IEnumerable<MordheimRoster.Models.Warband>

@{
    ViewBag.Title = "My Warbands";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gold)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wyrdstone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gold)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wyrdstone)
        </td>
        <td>
            @Html.ActionLink("Game", "GameMode", new { id = item.WarbandId }) |
            @Html.ActionLink("Edit", "Edit", new { id=item.WarbandId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WarbandId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WarbandId })
        </td>
    </tr>
}

</table>
